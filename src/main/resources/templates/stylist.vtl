<h3>$stylist.getName()</h3>
<h4>Shifts</h4>
$stylist.getWorkDays()
<br>

<h4>Clients</h4><a href="/stylists/$stylist.getId()/clients/new">ADD CLIENT</a>
#if ($clients.size() > 0)
  #foreach ($client in $clients)
    <p><a href="/stylists/$stylist.getId()/clients/$client.getId()">$client.getName()</a></p>
  #end
#end
